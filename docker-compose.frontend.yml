services:
  tarkov_frontend:
    container_name: tarkov_frontend
    image: ${FRONTEND_IMAGE:-luopc1218docker/tarkov-tactical-board-frontend:edge}
    restart: unless-stopped
    ports:
      - "127.0.0.1:18081:80"
